<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../style/listing.css">
    <title>All Combos</title>
</head>
<body>
    <h1>Akuma Combos</h1>
    <div class="links">
    <ul>
    <a href="/combos/character-select"> <li>Character Select </li></a>
    <a href="/combos/home"><li>Home </li></a>
    <a href="/combos/uploadKen"><li>Upload Combo</li></a>
    </ul>
    </div>
    <br>
    <br>
<div class="content">
    <% akumaCombos.forEach((oneCombo)=> {%>
        <div class="slot">
        <% if(oneCombo.character === "akuma" && oneCombo.video) {%>
            <p class="item">Character: <%= oneCombo.character %></p>
            <p class="item">Combo Starter: <%= oneCombo.starter %></p>
            <p class="item">Combo Description: <%= oneCombo.description %></p>
            <p class="item">Combo Resources: <%= oneCombo.resource %></p>
            <p class="item">User: <%= oneCombo.user?.username %></p>
            <iframe 
                src= "<%= oneCombo.video %>">
            </iframe>
    

        
        <% } %>
            
        <form method="post" action="/combos/<%= oneCombo._id %>?_method=DELETE">
        <br>
            <button type="submit">Delete Combo</button>
        </form>
        <br>
        <a href="/combos/akuma/<%= oneCombo._id %>/update">
        <button type="submit"> Update Combo</button>
        </a>
        <br>
        <br>
        <a href="/combos/<%= oneCombo._id %>"> Combo Details</a>
        
        
        <hr>
        </div>
    <% })  %>
    </div>
</body>
</html>